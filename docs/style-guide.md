# CIS133 Textbook Style Guide

This file holds the course-specific layer only. The canonical shared
writing law lives at `../shared/style-guide-core.md` in the textbooks
workspace. The sync script copies it into this repo as
`docs/style-guide-core.md`. Read the core first. Never edit the synced
copy, and never restate core rules here. When this file and the core
disagree, this file wins for CIS133 only.

Everything below is a placeholder. Replace each section when standing
up the course.

## Audience and Tone Calibration

Calibrate the core's tone invariants for community college students in their first web development course.

* **Assumed background:** [prerequisites completed, or "no prior
  experience". Name the specific courses.]
* **Register:** [for example: friendly but authoritative for intro
  students, collegial and professional for advanced students]
* **Reading level:** [Flesch Reading Ease target. The core default is
  60-70. Record any approved deviation and who approved it.]
* **Bridging rule:** [how to reference prerequisite content. Example:
  one-sentence bridge, never reteach.]

## Bloom's-Level Emphasis for CIS133

The shared framework lives in `docs/blooms-taxonomy-reference.md`
(synced). Record here how this course weights the six levels.

* **Primary levels:** [for example: Remember, Understand, Apply for an
  intro course]
* **Occasional levels:** [levels used only for specific module types]
* **Avoided levels:** [levels that under- or overshoot this audience,
  and why]
* **Distribution across the term:** [how the mix shifts from early to
  late modules]

## Tech-Stack Conventions for HTML5, CSS3, VS Code, browser DevTools

Language and tool rules the core delegates to the course guide.

* **Code fence identifiers:** [the exact identifiers used in this
  book, for example `python`, `sql`, `cpp`, `html`, `css`, `bash`]
* **Language style standard:** [PEP 8, SQL keyword casing, C++ brace
  style, CSS property ordering, or the equivalent]
* **Naming scheme:** [semantic naming rules and any required
  prefixes, with the allowed exceptions]
* **Version targets:** [tool and language versions all examples must
  run on]
* **Capitalization table:** [exact spellings for every tool and
  product name in this stack]

| Term | Correct | Incorrect |
| ---- | ------- | --------- |
| [tool] | [Correct Form] | [common wrong forms] |

## Dataset and Example-Domain Conventions

* **Example domains:** [the scenario families this book draws from,
  for example retail transactions, clinic scheduling, repair tickets]
* **Provided files:** [where lab files live, how students obtain
  them, and the one loading or setup pattern every lab uses]
* **Rules for new datasets or fixtures:** [minimum size, real versus
  synthetic, citation and license requirements, reproducibility
  requirements for generated files]
* **What students never hand-type:** [and the exceptions where typing
  the structure is itself the lesson]
